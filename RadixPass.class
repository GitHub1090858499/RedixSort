/******************************************************************************

Welcome to GDB Online.
GDB online is an online compiler and debugger tool for C, C++, Python, Java, PHP, Ruby, Perl,
C#, VB, Swift, Pascal, Fortran, Haskell, Objective-C, Assembly, HTML, CSS, JS, SQLite, Prolog.
Code, Compile, Run and Debug online from anywhere in world.

*******************************************************************************/




public class RadixPass
{
    
    
    public static void oneRadixPass(int[] a,int[] a2,int[] count,int[] pos,int radix){
        //位置、个数的数组置零
        Arrays.fill(count,0);
        Arrays.fill(pos,0);
        
        //遍历a的radix位,计数同数个数于count
        for(int i=0;i<a.length;i++){
            count[a[i]/redix%10]++; 
        }
        
        //遍历count，生成pos位置数组
        for(int i=1;i<count.length;i++){
            pos[i]=pos[i-1]+count[i-1];
        }
        
        //按pos位置信息的a的radix位摆放a到a2
        for(int i=0;i<a.length;i++){
            b[pos[a[i]/radix%10]]=a[i];
            pos[a[i]/radix%10]++;
        }
        
        
    }
    
	public static void main(String[] args) {
	    int[] a=[337,332,132,267,262,164,260,167];
	    int[] b=new int[8];
	    int[] count=new int[10];
	    int[] pos=new int[10];
	    int redix=1;
	    oneRadixPass(a,b,count,pos,redix);
	    System.out.println(b);
		System.out.println("Hello World");
	}
}
